name: Diagnostic

on:
  push:
    branches: [main, phase-0-fondations]

jobs:
  flutter:
    name: Flutter
    runs-on: ubuntu-latest
    if: ${{ hashFiles('app/pubspec.yaml') != '' }}
    steps:
      - uses: actions/checkout@v4
      - run: echo "condition évaluée"
